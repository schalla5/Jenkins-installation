---
- name: remove jenkins server
  hosts: all
  gather_facts: false
  user: "{{ uservar }}"
  sudo: true
  tasks:
      - name: Check that you can connect (GET) to a page and it returns a status 200
      - action: delete url=http://192.168.33.11:8080
