---
- name: remove jenkins server
  hosts: all
  gather_facts: false
  user: "{{ uservar }}"
  tasks:
      - name: remove jenkins server
        action: remove url:http://192.168.33.11:8080
        
