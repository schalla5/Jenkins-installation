---
- name: Create a ubuntu virtual machine via vagrant
  hosts: 127.0.0.1
  sudo: true
  tasks:

  	- name: change the directory path
  	  command: cd C:\tower2
  	- name: destroy the vagrant box
  	  command: vagrant destroy
